class Complexo:
def __init__(self, numero, real, imaginaria, complexo):
    self.numero = numero
    self.real = real
    self.imaginaria = imaginaria
    self.complexo = complexo

    def complexo(self):
        complexo = real + imaginaria

    def modulo(self, modulo):
        self.modulo = modulo
        modulo = |complexo| #como representar o modulo?

    def reset(self):
        real = real + 0.0
        imaginaria = imaginaria + 0.0

    def incrementar(self):
        real = real + 1
    
    def conjugado(self, conjugado):
        self.conjugado = z #como fazer esse conjugado?
    
    def __str__(self):
        #converter em string

    def soma(self, C, soma):
        self.C = C
        self.soma = soma
        soma = C + self
        return soma

        

